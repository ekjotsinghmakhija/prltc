# Codex CLI Hooks

## Specifics

- Prompt-level guidance via awareness document -- no programmatic hook
- `prltc-awareness.md` is injected into `AGENTS.md` with an `@PRLTC.md` reference
- Installed to `~/.codex/` by `prltc init --codex`
