name: Release
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  workflow_call:
    inputs:
      tag:
        description: 'Tag to release'
        required: true
        type: string
      prerelease:
        description: 'Mark as pre-release'
        required: false
        type: boolean
        default: false
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release (e.g., v0.1.0)'
        required: true
      prerelease:
        description: 'Mark as pre-release'
        type: boolean
        default: false

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    name: Build ${{ matrix.target }}
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # macOS
          - target: x86_64-apple-darwin
            os: macos-latest
            archive: tar.gz
          - target: aarch64-apple-darwin
            os: macos-latest
            archive: tar.gz
          # Linux
          - target: x86_64-unknown-linux-musl
            os: ubuntu-latest
            archive: tar.gz
            musl: true
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            archive: tar.gz
            cross: true
          # Windows
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            archive: zip

    steps:
      - name: Checkout
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
          fetch-tags: true

      - name: Install Rust
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross-compilation tools
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        if: matrix.cross
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu
          echo "CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER=aarch64-linux-gnu-gcc" >> $GITHUB_ENV

      - name: Install musl tools
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        if: matrix.musl
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-tools

      - name: Build
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        run: cargo build --release --target ${{ matrix.target }}
        env:
          PRLTC_TELEMETRY_URL: ${{ vars.PRLTC_TELEMETRY_URL }}
          PRLTC_TELEMETRY_TOKEN: ${{ secrets.PRLTC_TELEMETRY_TOKEN }}

      - name: Package (Unix)
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        if: matrix.os != 'windows-latest'
        run: |
          cd target/${{ matrix.target }}/release
          tar -czvf ../../../prltc-${{ matrix.target }}.${{ matrix.archive }} prltc
          cd ../../..

      - name: Package (Windows)
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        if: matrix.os == 'windows-latest'
        run: |
          cd target/${{ matrix.target }}/release
          7z a ../../../prltc-${{ matrix.target }}.${{ matrix.archive }} prltc.exe
          cd ../../..

      - name: Upload artifact
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        uses: actions/upload-artifact@v4
        with:
          name: prltc-${{ matrix.target }}
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
          path: prltc-${{ matrix.target }}.${{ matrix.archive }}

  build-deb:
    name: Build DEB package
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
          fetch-tags: true

      - name: Install Rust
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-deb
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        run: cargo install cargo-deb

      - name: Build DEB
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        run: cargo deb
        env:
          PRLTC_TELEMETRY_URL: ${{ vars.PRLTC_TELEMETRY_URL }}
          PRLTC_TELEMETRY_TOKEN: ${{ secrets.PRLTC_TELEMETRY_TOKEN }}

      - name: Upload DEB
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        uses: actions/upload-artifact@v4
        with:
          name: prltc-deb
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
          path: target/debian/*.deb

  build-rpm:
    name: Build RPM package
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    runs-on: ubuntu-latest
    container: fedora:latest
    steps:
      - name: Checkout
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
          fetch-tags: true

      - name: Install dependencies
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        run: |
          dnf install -y rust cargo rpm-build

      - name: Install cargo-generate-rpm
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        run: cargo install cargo-generate-rpm

      - name: Build release
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        run: cargo build --release
        env:
          PRLTC_TELEMETRY_URL: ${{ vars.PRLTC_TELEMETRY_URL }}
          PRLTC_TELEMETRY_TOKEN: ${{ secrets.PRLTC_TELEMETRY_TOKEN }}

      - name: Generate RPM
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        run: cargo generate-rpm

      - name: Upload RPM
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        uses: actions/upload-artifact@v4
        with:
          name: prltc-rpm
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
          path: target/generate-rpm/*.rpm

  release:
    name: Create Release
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    needs: [build, build-deb, build-rpm]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
          fetch-tags: true

      - name: Download all artifacts
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Get version
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        id: version
        run: |
          TAG="${{ inputs.tag }}"
          if [ -z "$TAG" ]; then
            TAG="${{ github.event.release.tag_name }}"
          fi
          echo "version=$TAG" >> $GITHUB_OUTPUT

      - name: Flatten artifacts
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        run: |
          mkdir -p release
          find artifacts -type f \( -name "*.tar.gz" -o -name "*.zip" -o -name "*.deb" -o -name "*.rpm" \) -exec cp {} release/ \;

      - name: Create version-agnostic package names
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        run: |
          cd release
          for f in *.deb; do
            [ -f "$f" ] && cp "$f" "prltc_amd64.deb"
          done
          for f in *.rpm; do
            [ -f "$f" ] && cp "$f" "prltc.x86_64.rpm"
          done

      - name: Create checksums
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        run: |
          cd release
          sha256sum * > checksums.txt

      - name: Upload Release Assets
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.version }}
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
          files: release/*
          prerelease: ${{ inputs.prerelease }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  notify-discord:
    name: Notify Discord
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    needs: [release]
    if: ${{ !inputs.prerelease }}
    runs-on: ubuntu-latest
    steps:
      - name: Get version
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        id: version
        run: |
          TAG="${{ inputs.tag }}"
          if [ -z "$TAG" ]; then
            TAG="${{ github.event.release.tag_name }}"
          fi
          echo "tag=$TAG" >> $GITHUB_OUTPUT

      - name: Send Discord notification
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        env:
          DISCORD_WEBHOOK: ${{ secrets.PRLTC_DISCORD_RELEASE }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ steps.version.outputs.tag }}"
          RELEASE_URL="https://github.com/ekjotsinghmakhija/prltc/releases/tag/${TAG}"

          # Fetch release notes from GitHub API
          NOTES=$(gh api "repos/ekjotsinghmakhija/prltc/releases/tags/${TAG}" --jq '.body' 2>/dev/null | head -c 1800 || echo "")
          DESC=$(echo "${NOTES:-No release notes}" | jq -Rs .)

          jq -n \
            --arg title "PRLTC ${TAG} released" \
            --arg url "$RELEASE_URL" \
            --argjson desc "$DESC" \
            '{embeds: [{title: $title, url: $url, description: $desc, color: 5814783, footer: {text: "Rust Token Killer"}}]}' \
          | curl -sf -H "Content-Type: application/json" -d @- "$DISCORD_WEBHOOK"

  homebrew:
    name: Update Homebrew formula
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    needs: [release]
    if: ${{ !inputs.prerelease }}
    runs-on: ubuntu-latest
    steps:
      - name: Get version
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        id: version
        run: |
          TAG="${{ inputs.tag }}"
          if [ -z "$TAG" ]; then
            TAG="${{ github.event.release.tag_name }}"
          fi
          VERSION="${TAG#v}"
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Download checksums
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        run: |
          gh release download "${{ steps.version.outputs.tag }}" \
            --repo ekjotsinghmakhija/prltc \
            --pattern checksums.txt
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Parse checksums
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        id: sha
        run: |
          echo "mac_arm=$(grep aarch64-apple-darwin.tar.gz checksums.txt | head -1 | awk '{print $1}')" >> $GITHUB_OUTPUT
          echo "mac_intel=$(grep x86_64-apple-darwin.tar.gz checksums.txt | head -1 | awk '{print $1}')" >> $GITHUB_OUTPUT
          echo "linux_arm=$(grep aarch64-unknown-linux-gnu.tar.gz checksums.txt | head -1 | awk '{print $1}')" >> $GITHUB_OUTPUT
          echo "linux_intel=$(grep x86_64-unknown-linux-musl.tar.gz checksums.txt | head -1 | awk '{print $1}')" >> $GITHUB_OUTPUT

      - name: Generate formula
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        run: |
          cat > prltc.rb << 'FORMULA'
          class Rtk < Formula
            desc "Rust Token Killer - High-performance CLI proxy to minimize LLM token consumption"
            homepage "https://www.github.com/ekjotsinghmakhija/prltc"
            version "VERSION_PLACEHOLDER"
            license "MIT"

            if OS.mac? && Hardware::CPU.arm?
              url "https://github.com/ekjotsinghmakhija/prltc/releases/download/TAG_PLACEHOLDER/prltc-aarch64-apple-darwin.tar.gz"
              sha256 "SHA_MAC_ARM_PLACEHOLDER"
            elsif OS.mac? && Hardware::CPU.intel?
              url "https://github.com/ekjotsinghmakhija/prltc/releases/download/TAG_PLACEHOLDER/prltc-x86_64-apple-darwin.tar.gz"
              sha256 "SHA_MAC_INTEL_PLACEHOLDER"
            elsif OS.linux? && Hardware::CPU.arm?
              url "https://github.com/ekjotsinghmakhija/prltc/releases/download/TAG_PLACEHOLDER/prltc-aarch64-unknown-linux-gnu.tar.gz"
              sha256 "SHA_LINUX_ARM_PLACEHOLDER"
            elsif OS.linux? && Hardware::CPU.intel?
              url "https://github.com/ekjotsinghmakhija/prltc/releases/download/TAG_PLACEHOLDER/prltc-x86_64-unknown-linux-musl.tar.gz"
              sha256 "SHA_LINUX_INTEL_PLACEHOLDER"
            end

            def install
              bin.install "prltc"
            end

            def caveats
              <<~EOS
                prltc is installed! Get started:

                  # Initialize for Claude Code
                  prltc init -g          # Global hook-first setup (recommended)
                  prltc init             # Add to ./CLAUDE.md (this project only)

                  # See all commands
                  prltc --help

                  # Measure your token savings
                  prltc gain

                Full documentation: https://www.github.com/ekjotsinghmakhija/prltc
              EOS
            end

            test do
              system "#{bin}/prltc", "--version"
            end
          end
          FORMULA
          sed -i "s/VERSION_PLACEHOLDER/${{ steps.version.outputs.version }}/g" prltc.rb
          sed -i "s/TAG_PLACEHOLDER/${{ steps.version.outputs.tag }}/g" prltc.rb
          sed -i "s/SHA_MAC_ARM_PLACEHOLDER/${{ steps.sha.outputs.mac_arm }}/g" prltc.rb
          sed -i "s/SHA_MAC_INTEL_PLACEHOLDER/${{ steps.sha.outputs.mac_intel }}/g" prltc.rb
          sed -i "s/SHA_LINUX_ARM_PLACEHOLDER/${{ steps.sha.outputs.linux_arm }}/g" prltc.rb
          sed -i "s/SHA_LINUX_INTEL_PLACEHOLDER/${{ steps.sha.outputs.linux_intel }}/g" prltc.rb
          # Remove leading spaces from heredoc
          sed -i 's/^          //' prltc.rb

      - name: Push to homebrew-tap
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        run: |
          CONTENT=$(base64 -w 0 prltc.rb)
          SHA=$(gh api repos/ekjotsinghmakhija/homebrew-tap/contents/Formula/prltc.rb --jq '.sha' 2>/dev/null || echo "")
          if [ -n "$SHA" ]; then
            gh api -X PUT repos/ekjotsinghmakhija/homebrew-tap/contents/Formula/prltc.rb \
              -f message="prltc ${{ steps.version.outputs.version }}" \
              -f content="$CONTENT" \
              -f sha="$SHA"
          else
            gh api -X PUT repos/ekjotsinghmakhija/homebrew-tap/contents/Formula/prltc.rb \
              -f message="prltc ${{ steps.version.outputs.version }}" \
              -f content="$CONTENT"
          fi
        env:
          GH_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
